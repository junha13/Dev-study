<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-emp">
	<select id="getEmpFirstNLastName" resultType="myspring.Emp" parameterType="myspring.Emp">
		select last_name
			, first_name
			, employee_id
		from employees
		where 1 = 1
		<choose>
			<when test="firstName !=null">
		and first_name = #{firstName}</when>
			<when test="lastName !=null">
		and last_name = #{lastName}</when>
		</choose>
	</select>
	
	
	<select id="getEmpById" resultType="myspring.Emp">
		select last_name
			, first_name
			, employee_id
		from employees
		where employee_id = #{id}
	</select>

	<select id="getEmpListByJobId" resultType="myspring.Emp">
		select last_name
			, first_name
			, employee_id
		from employees
		where job_id = #{JobId}
	</select>
	
	<select id="getEmpListByDeptNo" resultType="myspring.Emp">
		select last_name
			, first_name
			, employee_id
			, salary
		from employees
		where department_id = #{deptId}
	</select>
	
	<!-- 하나는 emp, 하나는 emplist 지만 resultType은 resultType="mybatis.Emp" 이렇게 동일하게 써도 됨 -->

</mapper>