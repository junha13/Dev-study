<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper-emp">
	<select id="getEmpById" resultType="mybatis.Emp">
		select last_name lastName
			, first_name firstName
			, employee_id id
		from employees
		where employee_id = #{id}
	</select>
	<select id="getEmpListByJobId" resultType="mybatis.Emp">
		select last_name lastName
			, first_name firstName
			, employee_id id
		from employees
		where job_id = #{JobId}
	</select>
	
	<!-- 하나는 emp, 하나는 emplist 지만 resultType은 resultType="mybatis.Emp" 이렇게 동일하게 써도 됨 -->

</mapper>